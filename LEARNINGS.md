# LEARNINGS

개발 이터레이션에서 실제로 발견된 문제 패턴과 해결책의 증류본.
새 이터레이션에서 같은 함정을 밟기 전에 먼저 읽을 것.

## 1. 도메인 로직 — 시간·상태

### 자정 롤오버: "현재 날짜"를 캐시하면 언젠가 틀린다
- **문제**: `recs.today`를 부팅 시 1회 캐시 → 페이지를 자정 넘겨 열어두면 새 날짜의 런이 어제 키에 저장돼 streak이 끊김. 결정적으로, **새로 추가한 카운트다운 기능이 사용자를 자정까지 대기하도록 유도**해 잠재 버그를 현실 버그로 격상시킴.
- **해결**: ① 기록은 "지금 날짜"가 아니라 **런의 시드 날짜**(`res.seedStr`)로 저장 — 데이터가 자신의 출처를 들고 다니게 한다. ② 경계 이벤트(런 시작, 게임 오버, 카운트다운 0초)마다 `syncToday()`로 캐시 갱신.
- **교훈**: 기능 추가는 기존 가정의 노출 빈도를 바꾼다. "그 시나리오는 드물다"는 가정은 그 시나리오를 유도하는 기능이 들어오는 순간 무효.

### 일시정지의 진짜 어려움은 멈춤이 아니라 입력 누수
- **문제 패턴**: ① blur로 자동 일시정지되면 keyup이 영원히 안 옴 → 키가 눌린 채 고정. ② 오버레이 위에서 누른 키가 재개 직후 반영. ③ 조이스틱 드래그 중 일시정지 시 축이 남음. ④ 메뉴에서 blur된 키가 **다음 런**까지 살아남음.
- **해결**: 상태 전이 지점마다 입력을 리셋 — `pause()`, `resume()`, **그리고 `start()`**에서 `resetKeys()` + 축 0. 전이마다 "이 시점에 살아있으면 안 되는 입력은 무엇인가"를 물을 것.

### 중도 포기(QUIT)는 "기록 경로를 안 타는 것"으로 구현
- `endGame()`을 경유하지 않고 `toMenu()`로 직행하면 onGameOver·저장이 자연히 생략됨. 별도 "discard 플래그"보다 경로 분리가 단순하고 테스트하기 쉬움 (spy 호출 0 + IndexedDB 불변 assert).

## 2. 헤드리스 브라우저 테스트

### 가상 시간(virtual-time-budget)에서 rAF는 굶는다
- **증상**: 타이머(setTimeout/setInterval)는 빨리 감기되는데 requestAnimationFrame 루프는 거의 안 돎 → 게임이 ready에서 영원히 정지.
- **해결**: 테스트 하니스가 `G.update(dt)`를 직접 호출해 시뮬을 수동 스텝. 엔진을 rAF에서 분리 가능하게 설계(`G.update` 공개)한 것이 테스트 가능성의 핵심이었음.

### 정적 서버의 cleanUrls가 쿼리스트링을 삼킨다
- `npx serve`는 `/page.html?s=x` 리다이렉트 과정에서 쿼리를 유실 → 하니스 시나리오 라우팅이 조용히 기본값으로 떨어짐. **해시(`#scenario`)는 리다이렉트에도 살아남는다.**
- 디버깅법: 상태를 화면 텍스트로 렌더링하고 스크린샷으로 읽기 (`--dump-dom`은 환경에 따라 무출력).

### 헤드리스 Edge에는 최소 창 폭(~500px)이 있다
- **증상**: `--window-size=390,844`로 모바일 폭 스크린샷을 찍으면 실제 뷰포트는 ~500px로
  렌더링되고 PNG만 390px로 잘림 → 중앙 정렬 콘텐츠가 오른쪽으로 치우쳐 "오버플로 버그"처럼 보임.
  페이지 내부 실측(`innerWidth`, `scrollWidth`)은 정상이었음.
- **해결**: 모바일 뷰포트 검증은 **고정 크기 iframe** 안에서 (E2E 하니스 방식). 스크린샷이
  이상하면 스크린샷을 믿기 전에 페이지 내부 측정값부터 확인할 것.

### 자명하게 통과하는 어서션은 통과가 아니다
- "브레이크다운 합 == 점수"가 0 == 0으로 통과, "스파크라인 not blank"가 평평한 기준선으로 통과. **assert가 비자명한 입력 위에서 도는지 출력 로그로 확인할 것** (`score=348` 같은 디테일을 리포트에 포함). 결정적 픽스처 주입(플레이어 위치에 크리스털 강제 스폰)으로 flake 없이 비자명성 확보.

## 3. 테스트 설계

### IIFE 게임 코드도 의존성 0으로 단위 테스트 가능
- `vm.createContext` 샌드박스에 `window = sandbox` + 리스너 캡처 + `SY.audio`는 Proxy noop. IndexedDB는 사용하는 API 표면만(open/transaction/get/put) ~50줄로 스텁. 시계는 `Date` 서브클래스로 동결 — 월/연 경계 픽스처가 공짜.
- **함정**: vm realm의 Array는 호스트 `deepStrictEqual`에서 프로토타입 불일치로 실패 → `Array.from()`으로 호스트 realm 복사 후 비교.

### 컨벤션은 tripwire 테스트로 강제해야 산다
- "게임플레이 랜덤은 시드 RNG만", "innerHTML 싱크 추가 금지", "인라인 style 금지" 같은 규칙은 문서만으로는 자신도 어긴다(이번에 인라인 style 3건 자가 위반). **개수 베이스라인 핀**(Math.random ≤14, innerHTML 싱크 == 3, inline style ≤6)으로 위반 시 테스트가 강제 리뷰를 발동하게 할 것.

### 생성 산출물 동기화는 "커밋 독립적" 검사로
- `git diff --exit-code`는 커밋 전엔 항상 실패하는 잘못된 검사. **임시 경로로 fresh build → 해시 비교**가 올바른 "재현 가능 + 동기화" 검사. 훅(직접 수정 차단 + stale 리마인드)과 합쳐 3중 방어.

### 독립 Verifier는 all-green 뒤의 버그를 찾는다
- 테스트 전부 통과 상태에서 적대적 리뷰 에이전트가 실질 버그(자정 롤오버)와 규칙 위반(인라인 style)을 적발. 구현자와 같은 컨텍스트의 셀프 리뷰는 같은 맹점을 공유함 — **"기준을 위반하는 방법을 적극적으로 찾아라"는 프롬프트가 유효**했음.

### 시간 게이트 콘텐츠 테스트는 파일럿을 살려둬야 한다
- 엘리트(~t=24s)·1UP·보스(마지막 20s)처럼 **시간으로 게이트된 스폰**을 입력 없는 패시브 런으로 테스트하면, 게이트 도달 전에 기뢰/적에 맞아 죽어(`phase='over'`) 시간이 멈춤 → 게이트가 **한 번도 관측 안 됨**인데 테스트는 자명 통과. 매 프레임 `s.player.inv = 1`(+ hp 보강)로 살려둬야 게이트가 실제로 발화함.

### 희귀 이벤트 공정성 테스트는 "발생했는지"를 단언하라
- "같은 시드 → 동일 X" 테스트에서 X가 희귀하면, 창 안에서 한 번도 안 떠도 `none==none`으로 통과. 결정성만 보고 커버리지는 0. **이벤트가 실제로 발생했음을 함께 단언**(`firstAt >= 0`)해야 의미 있는 비교가 됨(1UP/엘리트 fairness 테스트에 적용).

### 점수 합 property 테스트는 보스/엘리트 구간까지 돌려야 한다
- d=20 짧은 런은 보스(d≥40)·엘리트 미도달 → 신규 가산 경로(엘리트 250, 보스 칩)가 "버킷 합 ≡ 총점" 불변식 **안에서 검증 안 됨**. d=60 풀런(파일럿 생존) 테스트를 별도로 둬 boss·destruction 버킷을 sum 단언 안에 포함.

## 4. UI 패턴

### CSS 기본값이 `display:none`이면 JS의 `''` 복원이 안 통한다
- `el.style.display = ''`는 인라인 제거 → CSS 기본값(none)으로 폴백 → 영원히 안 보임. 같은 버그를 `#btn-pause`와 `#menu-week`에서 두 번 만남. **숨김 기본 + JS 토글 요소는 표시 값('block')을 명시**하거나, 클래스 토글로 통일할 것.

### 지연 표시되는 화면의 단축키는 phase가 아니라 DOM visibility로 가드
- 게임 오버 화면은 phase 전환 650ms 후 표시 → phase 기준이면 화면이 뜨기 전에 R/Enter가 동작. `classList.contains('visible')` 체크가 정답. keydown 분기 순서(over > howto > paused > playing)로 Esc의 다중 역할 충돌 해소.

## 5. Windows PowerShell 5.1 (이 프로젝트의 셸 환경)

| 함정 | 해결 |
|---|---|
| `Get-Content`가 BOM 없는 UTF-8을 ANSI로 읽음 → **한글 파일 통째로 깨짐** (실제 사고, git으로 복원) | 비ASCII 파일은 반드시 `[IO.File]::ReadAllText($p, [Text.Encoding]::UTF8)` / `WriteAllText` |
| 내부 따옴표 포함 멀티라인 인자가 native 명령에서 깨짐 (`git commit -m` 실패) | 메시지를 파일로 쓰고 `git commit -F` |
| `Process.WaitForExit(timeout)` 후 `ExitCode`가 null | `HasExited` 확인 후 **인자 없는** `WaitForExit()` 한 번 더 |
| `$profile`은 자동 변수 — 대입 시 부작용 | 변수명 회피 (`$edgeProfile`) |
| **`Start-Process -PassThru`의 `.ExitCode`가 `$null`** → `exit $null` = exit 0. E2E가 실패해도 스위트가 ALL PASS로 통과되는 침묵 버그(여러 커밋 동안 게이트가 무력) | Start-Process 직후 `$server.Handle \| Out-Null`로 핸들 캐시 후 `WaitForExit()` → `.ExitCode` 신뢰 가능. null이면 명시적 실패 처리 |
| **대소문자 미구분 파일시스템(`/mnt/c`, Windows): `DESIGN.md` ≡ `design.md`가 같은 파일** — 디자인 시스템 문서를 `DESIGN.md`로 만들면 아키텍처 개요(`design.md`)를 통째로 덮어씀 | 디자인 시스템 문서는 `docs/design-system.md`로 분리. 새 문서 생성 전 대소문자만 다른 기존 파일이 있는지 확인 |

## 7. Playwright / headless 검증

### 백그라운드 탭에서 rAF는 스로틀된다 — DOM 검증은 스크린샷으로
- **증상**: Playwright로 게임 상태를 주입(`s.fx.X2=7` 등)하고 `browser_evaluate`로 HUD DOM을
  읽으면 0/미반영이 나옴. 게임 루프(`requestAnimationFrame`)가 백그라운드/비페인트 탭에서
  거의 안 도므로 `updateHud`가 실행되지 않아서임.
- **해결**: HUD처럼 rAF 루프가 갱신하는 DOM은 **스크린샷으로 검증**(스크린샷은 페인트=프레임을
  강제). 또는 deterministic하게 만들려면 엔진의 `update`/HUD 갱신을 수동 호출 가능하게 설계.
  E2E 회귀 가드는 rAF 비의존(버튼 클릭→async 렌더 폴링) 경로로 작성.

### CSS/HTML 변경은 리로드해야 반영된다
- 라이브 페이지에서 CSS 파일을 편집해도 열려 있는 탭은 옛 스타일을 유지한다. 레이아웃 수정을
  검증하려면 반드시 `browser_navigate`로 리로드 후 스크린샷. (옛 CSS로 "안 고쳐졌다"고 오판한 적 있음.)

### 비결정 async 렌더는 고정 sleep 말고 폴링
- IndexedDB 기반 `renderRecords` 같은 async 렌더를 고정 `sleep(250)`으로 기다리면 가상시간
  스케줄링 편차로 간헐 실패. 조건 폴링(`while(!done && t<2s) sleep`)으로 안정화.
| `Out-File`/`Set-Content` 기본 인코딩이 UTF-16/BOM | 다른 도구가 읽을 파일은 인코딩 명시 |

## 6. 프로세스

- **시드 RNG 게임의 점수 집계는 버킷 분해 + "합 ≡ 총점" 불변식**으로 설계하면 property 테스트 한 방으로 모든 가산 경로를 커버한다 (×2 배율도 버킷 안에서 적용해야 불변식 유지).
- **결정적 시뮬은 E2E 재현성의 토대**: 날짜 시드 + 수동 스텝 → 같은 테스트가 항상 같은 점수(348)를 산출. flaky 어서션이 원천 차단됨.
- 훅 리마인더(소스 변경 → standalone stale)는 시끄럽지만 실제로 재빌드 누락을 한 번도 허용하지 않았음 — 노이즈보다 가드 가치가 컸다.

## 7. 스프라이트 아틀라스 (rect 추출·활용 측정)

### Hollow 네온 line-art는 connected-component로 과소 검출된다
- 속이 빈(테두리만 밝은) 프레임·해골·링 스프라이트는 픽셀이 끊겨 CC가 여러 조각으로 쪼갬. **dilated gap(이웃 2~6px)**으로 BFS해야 하나로 묶임. 그래도 분절되면 그리드 좌표를 띄워 **흰/어두운 배경 양쪽에 크롭해 육안 확정**이 가장 확실(메모리: T=80 matte 작업과 동일 교훈).
- 추출한 rect는 그릴 때 회전 정렬에 주의: 세로 스프라이트(탄환·미사일·빔 기둥)는 속도각에 `+ Math.PI/2` 오프셋 필요.

### "아틀라스 전체 사용?" 픽셀 측정은 글로우 후광에 부풀려진다
- 사용 rect로 덮인 불투명 픽셀 비율로 활용도를 재면, 모든 네온 스프라이트의 **글로우 후광이 타이트 rect 밖으로 번져** "미사용"으로 집계 → 실제 스프라이트 사용률보다 낮게 나옴. 정확한 활용 평가는 **개별 스프라이트(rect) 단위로 "이 sprite가 코드에서 참조되나"**를 세는 게 맞고, 픽셀%는 하한 근사로만.
- 8섹션이 각각 대표 시스템으로 흡수돼도 픽셀%는 ~42%에 그칠 수 있음(섹션당 다수 변형 스프라이트 존재). "섹션 커버리지"와 "픽셀 활용률"은 다른 지표.

## 8. 60fps 핫패스 (렌더)

### `ctx.setLineDash([...])`는 호출마다 배열을 새로 할당한다
- 텔레그래프 점선 등에서 `ctx.setLineDash([10,8])`를 프레임마다 호출하면 매번 2-요소 배열 GC. **모듈 상수로 호이스팅**(`const BEAM_DASH=[10,8]`) 후 전달. save/restore가 dash 상태를 복원하므로 리셋 불필요. (performance-analyzer가 적발 — 사소하지만 진짜 프레임당 할당.)
- 같은 맥락: 회전 빔/광선은 `createLinearGradient`(프레임당 객체) 대신 **상수 rgba 문자열 fillRect 2~3겹**으로 글로우를 흉내내면 무할당.

## 9. 헤드리스 렌더 갤러리 (시각 검증 도구)

### 헤드리스에서 게임 캔버스는 `SY.nvRender()`로 직접 페인트
- 가상시간/실시간 헤드리스 모두 rAF가 굶어 셸 루프가 게임 캔버스를 안 그림. 시각 캡처는 상태를 셋업한 뒤 **`w.SY.nvRender(canvas.getContext('2d'))`를 직접 호출** → `canvas.toDataURL()`로 PNG 추출(서버에 POST 저장). E2E 단언("not blank")으론 못 잡는 보스 형상·빔·프레임 정합을 사람이 눈으로 확인.
### 갤러리 러너의 두 함정 (gallery.sh에서 실제로 겪음)
- **chrome 수명:** `--virtual-time-budget` 없이 헤드리스 chrome는 스스로 종료 안 함 → 포그라운드로 띄우면 러너가 멈춤. **chrome를 백그라운드(&)로 띄우고 서버의 `/done`을 `wait`** 하거나, run.sh처럼 virtual-time-budget으로 chrome가 종료되게 할 것.
- **로컬 서버 바인딩:** Node `server.listen(port)`(host 생략)이 WSL에서 IPv6-only로 바인딩돼 `127.0.0.1`(IPv4) 접속이 거부될 수 있음 → **`listen(port, '127.0.0.1', …)` 명시**. 또 정적 핸들러는 `readFile`를 **먼저** await하고 그 다음 `writeHead` — 404가 헤더 이중 전송(`ERR_HTTP_HEADERS_SENT`)으로 서버를 죽이는 것 방지.
### 플레이어가 안 그려지면 무적 깜빡임을 의심
- `drawPlayer`는 `p.inv>0 && floor(inv*12)%2===0`이면 그리기를 건너뜀(피격 깜빡임). 시각 캡처/검증에서 플레이어가 사라지면 **`s.player.inv=0`**으로 두라 — 제품 버그로 오인하기 쉬움.
